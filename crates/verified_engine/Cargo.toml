[package]
name = "verified_engine"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true, features = ["derive"] }
verified_engine_macros = { version = "0.1.0", path = "../verified_engine_macros" }
[dev-dependencies]
approx = { workspace = true }

[lints]
workspace = true
