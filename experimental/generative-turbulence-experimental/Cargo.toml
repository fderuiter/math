[package]
name = "generative-turbulence-experimental"
version = "0.1.0"
edition = "2024"

[dependencies]

math_explorer = { path = "../../math_explorer" }
approx = "0.5.1"
pure_math = { path = "../../crates/pure_math" }
[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tch = "0.15.0"
