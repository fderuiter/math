## 2026-04-17 - ReactionDiffusionSystemBuilder | Problem: Primitive Obsession and Panic-Prone Construction | Decision: Apply the Builder Pattern | Consequence: Removes `unwrap` and `expect` from the library code, converting panics into explicit `ReactionDiffusionError` results. Removes primitive obsession (`usize` mix-ups). Required a semantic version bump to 0.4.0 due to the removal of `new` and `new_with_solver`.
